const {chromium}=require('playwright'),assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'chrome',headless:true});try{
const page=await browser.newPage({viewport:{width:390,height:844}}),errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.goto('http://localhost:3457/#/bibliothek?subject=en');await page.locator('[data-library-search]').waitFor();assert.equal(await page.locator('.library-topic').count(),30);
const before=await page.evaluate(()=>JSON.stringify(Store.load().stats));
await page.locator('[data-library-search]').fill('Oxford');assert.equal(await page.locator('.library-topic').count(),8);
await page.locator('.library-topic h3 a[href*="read-dictionary"]').click();await page.locator('.library-reader').waitFor();assert.ok(page.url().includes('q=Oxford'));assert.equal(await page.locator('.library-glossary dt').count(),3);await page.locator('[data-reading-jump]').click();assert.ok(await page.evaluate(()=>document.activeElement.hasAttribute('data-library-example')));assert.equal(await page.locator('[data-library-reading] input,[data-library-reading] textarea,[data-library-reading] button').count(),0);
await page.locator('[data-save-topic]').click();await page.reload();await page.locator('.library-reader').waitFor();assert.equal(await page.locator('[data-save-topic]').getAttribute('aria-pressed'),'true');
assert.ok(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await page.locator('.library-reading-section').first().scrollIntoViewIfNeeded();await page.screenshot({path:'/tmp/wunder-library-article.png'});
await page.locator('.library-reader > a').first().click();await page.locator('[data-library-search]').waitFor();assert.equal(await page.locator('[data-library-search]').inputValue(),'Oxford');
await page.locator('[data-library-saved]').check();assert.equal(await page.locator('.library-topic').count(),1);
await page.locator('.library-topic h3 a').click();await page.locator('.library-reader').waitFor();await page.locator('[data-save-topic]').click();await page.locator('.library-reader > a').first().click();await page.locator('[data-library-search]').waitFor();assert.equal(await page.locator('.library-topic').count(),0);
for(const subject of ['de','math']){await page.goto('http://localhost:3457/#/bibliothek?subject='+subject);await page.locator('.library-topic').first().waitFor();assert.equal(await page.locator('.library-topic').count(),subject==='de'?29:45);await page.locator('.library-topic h3 a').first().click();await page.locator('.library-reader').waitFor();assert.equal(await page.locator('#page-root').getAttribute('lang'),'de');assert.ok(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));}
await page.goto('http://localhost:3457/#/bibliothek/read-tangente?subject=math&section=examples');await page.locator('[data-library-example]').first().waitFor();await page.waitForFunction(()=>document.activeElement.hasAttribute('data-library-example'));assert.ok(await page.locator('.library-example-links a[href*="musteraufgabe1a"]').count());assert.ok(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));assert.equal(await page.evaluate(()=>JSON.stringify(Store.load().stats)),before);assert.deepEqual(errors,[]);console.log('PASS: library search by source, article navigation, saved filter and bookmarks across reload, mobile reading, unchanged scores, no page errors');
}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
